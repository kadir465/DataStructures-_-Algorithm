#include<stdio.h>
int factorial(int sayi){
if((sayi==1) || (sayi==0))
    return 1;
  else {
  return n*(n-1);
  }
}
int main(){
int i,sayi;
  print("faktoriyeli hesaplanmasını istediğiniz sayıyı giriniz ");
  scanf("%d",&sayi);
  print("the factorial of your number %d",factorial(sayi));
}
